package org.koreait.board.services;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

public class BoardRegisterServiceTest {

    private BoardRegisterService service;

    @BeforeEach
    void init(){
        //BoardMapper mapper = DBConn.getInstance().getSession().getMapper(BoardMapper.class);
        //BoardValidator validator = new BoardRegisterService(mapper);
        //service = new BoardRegisterService(validator, mapper);
    }

    @Test
    void test1(){

    }
}
